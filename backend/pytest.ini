[pytest]
# Mode auto : les tests `async def` s'exécutent sans décorateur explicite
# (pytest-asyncio). Les marqueurs @pytest.mark.asyncio restent tolérés.
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
